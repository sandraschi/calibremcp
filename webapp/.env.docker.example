# Docker Compose environment
# Copy to .env and set your Calibre library path

# Required: Path to your Calibre library on the host
CALIBRE_LIBRARY_PATH=L:/Multimedia Files/Written Word/Calibre-Bibliothek

# Optional: API URL for browser (default works for localhost)
NEXT_PUBLIC_API_URL=http://localhost:13000
